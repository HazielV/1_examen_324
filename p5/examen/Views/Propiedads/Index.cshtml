@model IEnumerable<examen.Models.Propiedad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoCatastral)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdImpuestoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoCatastral)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdImpuestoNavigation.IdImpuesto)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPropiedad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPropiedad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPropiedad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
